import SwiftUI

extension Font {
    static func ADLaMDisplay(size: CGFloat) -> Font {
        return .custom("ADLaMDisplay-Regular", fixedSize: size)
    }
    
    static func arial(size: CGFloat) -> Font {
        return .custom("Arial", fixedSize: size)
    }
}
